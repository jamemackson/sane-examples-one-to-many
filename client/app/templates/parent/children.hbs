<div class="row">
    {{#link-to 'parent.children.new'}}Create New Child{{/link-to}}
</div>
<div class="row">
    {{#each model as |child|}}

        <p>{{child.name}}: {{child.age}}</p>

    {{/each}}
</div>
<div class="row">
{{outlet}}
</div>